/** To get the actually implemented version (which may differ per each platform
    and also depends on the actual dymaic library you are linking to),
    use ::fj_platform::version. */
/// \begin{version,Library header version}
// clang-format off
#define FEJIX_VERSION_MAJOR (@fejix_VERSION_MAJOR@)
#define FEJIX_VERSION_MINOR (@fejix_VERSION_MINOR@)
#define FEJIX_VERSION_PATCH (@fejix_VERSION_PATCH@)
// clang-format on
/// \end
