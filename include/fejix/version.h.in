/** To get the actually implemented version (which may differ per each platform
    and also depends on the actual dymaic library you are linking to),
    use fj_platform::version. */
/// \BEGIN{version_macros,Library header version}
// clang-format off
#define FJ_VERSION_MAJOR (@fejix_VERSION_MAJOR@)
#define FJ_VERSION_MINOR (@fejix_VERSION_MINOR@)
#define FJ_VERSION_PATCH (@fejix_VERSION_PATCH@)
// clang-format on
/// \END
